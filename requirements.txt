# SAGE — Python dependencies
# All versions pinned. Verify latest on PyPI before updating.

# Environment
python-dotenv>=1.2.2  # SAGE: CVE-2026-28684

# HTTP client — NVD API calls
requests==2.32.3

# Version matching — PEP 440 compliant (never manual string comparison)
packaging==24.1

# LLM — analyzer + patch generator
anthropic==0.40.0
google-genai>=2.8.0      # Gemini API (primary analyzer) — replaces deprecated google-generativeai

# GitHub PR automation
PyGithub==2.5.0

# Static analysis
semgrep==1.90.0

# Knowledge graph
networkx==3.4.2

# JSON schema validation — defend against malformed CVE data
jsonschema==4.23.0

# Database
# sqlite3 is built into Python stdlib — no install needed

# Synapse — codebase parsing + knowledge graph
tree-sitter==0.23.2           # AST parser core
tree-sitter-python==0.23.6    # Python language grammar
tree-sitter-javascript==0.23.1  # JS/JSX grammar — must match tree-sitter core 0.23.x
tree-sitter-typescript==0.23.2  # TS/TSX grammar — must match tree-sitter core 0.23.x
